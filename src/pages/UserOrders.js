const UserOrders = (props) => {

    const orders = {...props.userOrders};
    console.log(orders);
    return (
        <div>
            Tutaj ORDER TEST
        </div>
    )
}
export default UserOrders;